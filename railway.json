{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "cd admin-backend && npm ci && npm run build"
    },
    "deploy": {
      "startCommand": "cd admin-backend && npm run start",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
    }
  }